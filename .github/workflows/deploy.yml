# /.github/workflows/deploy.yml
# v2.0 - 2025-08-07 12:10:00 - Corrige execução do init_admin.py
# v2.1 - 2025-08-07 - Padroniza para 'docker compose' e ajusta contextos/endpoints.

name: Deploy to VPS

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout do código
      uses: actions/checkout@v3

    - name: Iniciar serviços com Docker Compose
      run: |
        # Usa o docker-compose.yml corrigido para iniciar os serviços
        docker compose up -d --build --remove-orphans
        
    - name: Executar testes de integração e saúde
      run: |
        # Espera a API ficar saudável antes de rodar os testes
        echo "⏳ Aguardando saúde da API..."
        # O loop agora verifica o health check por 60 segundos (10 tentativas de 15s)
        for i in {1..10}; do
          # Verifica o endpoint raiz, que é o alvo do nosso healthcheck
          if docker compose ps | grep 'setormusicalms-backend' | grep -q '(healthy)'; then
            echo "✅ API saudável (status healthy)!"
            break
          fi
          echo "Aguardando contêiner backend ficar saudável..."
          sleep 15
        done

        # Verificação final do status
        docker compose ps
        
        echo "🚀 Inicializando admin e testando endpoint..."
        # Executa o script de inicialização do admin com Python diretamente
        docker compose exec -T backend python /app/init_admin.py || echo "⚠️ Falha na inicialização do admin, continuando..."
        
        echo "🧪 Testando endpoints da API..."
        # Testa endpoint básico (raiz)
        curl -f http://localhost:8000/ || (echo "❌ Falha no endpoint raiz" && exit 1 )
        
        echo "✅ Testes concluídos!"

    - name: Encerrar containers após testes
      if: always() # Garante que este passo rode mesmo se os testes falharem
      run: docker compose down

  build_and_push:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: Checkout do código
      uses: actions/checkout@v3
      
    - name: Login no Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Build e push da imagem do backend
      uses: docker/build-push-action@v4
      with:
        context: . # CORRIGIDO: O Dockerfile do backend está na raiz
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/setormusicalms-backend:latest
        
    - name: Build e push da imagem do frontend
      uses: docker/build-push-action@v4
      with:
        context: ./frontend
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/setormusicalms-frontend:latest

  deploy_and_validate:
    needs: build_and_push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: Deploy na VPS
      uses: appleboy/ssh-action@v1.0.3 # Versão atualizada do action
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          echo "🚀 Iniciando deploy na VPS..."
          
          # Navega para o diretório do projeto
          cd /root/setormusicalms || { echo "❌ Diretório não encontrado"; exit 1; }
          
          # Atualiza o código do repositório
          echo "📥 Atualizando código..."
          git pull origin main
          
          # Para os containers existentes
          echo "🛑 Parando containers..."
          docker compose down || echo "⚠️ Nenhum container rodando"
          
          # Remove imagens antigas para forçar download das novas
          echo "🗑️ Removendo imagens antigas..."
          docker rmi ${{ secrets.DOCKER_USERNAME }}/setormusicalms-backend:latest || echo "⚠️ Imagem backend não encontrada para remover"
          docker rmi ${{ secrets.DOCKER_USERNAME }}/setormusicalms-frontend:latest || echo "⚠️ Imagem frontend não encontrada para remover"
          
          # Faz pull das novas imagens do Docker Hub
          echo "📦 Baixando novas imagens..."
          docker pull ${{ secrets.DOCKER_USERNAME }}/setormusicalms-backend:latest
          docker pull ${{ secrets.DOCKER_USERNAME }}/setormusicalms-frontend:latest
          
          # Inicia os novos containers em modo detached
          echo "🚀 Iniciando novos containers..."
          docker compose up -d
          
          # Aguarda os serviços ficarem prontos
          echo "⏳ Aguardando serviços..."
          sleep 40
          
          # Executa inicialização do admin
          echo "👤 Inicializando usuário admin..."
          docker compose exec -T backend python /app/init_admin.py || echo "⚠️ Falha na inicialização do admin"
          
          # Verifica se os serviços estão rodando
          echo "🔍 Verificando status dos serviços..."
          docker compose ps
          
          echo "✅ Deploy concluído!"
          
    - name: Validar deploy
      run: |
        echo "🧪 Validando deploy em setormusicalms.art.br..."
        
        # Aguarda um pouco para os serviços estabilizarem
        sleep 20
        
        # Testa se o site está acessível
        for i in {1..5}; do
          if curl -f -s http://setormusicalms.art.br/ >/dev/null 2>&1; then
            echo "✅ Site acessível!"
            exit 0 # Sucesso, sai do script
          fi
          echo "⏳ Tentativa $i/5 - aguardando site ficar disponível..."
          sleep 20
        done

        echo "❌ Falha ao validar o deploy. O site não ficou acessível."
        exit 1
